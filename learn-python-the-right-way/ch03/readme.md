If you got following error when trying the code

```
ModuleNotFoundError: No module named '_tkinter'
```

Then, you need to install `python-tk`. On macos, run following:

```sh
brew install python-tk@3.9
```
